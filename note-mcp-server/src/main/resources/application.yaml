spring:
  application:
    name: note-mcp-server
  ai:
    mcp:
      server:
        name: note-mcp-server
        version: 0.0.1
        protocol: streamable

  datasource:
    url: jdbc:h2:mem:note_mcp_server_db

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8081
